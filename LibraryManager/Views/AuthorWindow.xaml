<Window x:Class="LibraryManager.Views.AuthorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами" Height="500" Width="700" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- DataGrid с авторами -->
        <DataGrid Grid.Row="0" x:Name="DataGridAuthors" AutoGenerateColumns="False" Margin="10,10,10,10"
                  ItemsSource="{Binding Authors}" SelectedItem="{Binding SelectedAuthor}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="150"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="150"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" Width="120"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0,10,10" Height="40">
            <Button Content="+ Добавить" Width="100" Click="AddAuthor_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button Content="✎ Редактировать" Width="120" Click="EditAuthor_Click" Background="#FF9800" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button Content="✕ Удалить" Width="100" Click="DeleteAuthor_Click" Background="#F44336" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button Content="Закрыть" Width="100" Click="Close_Click" IsCancel="True" Margin="20,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>