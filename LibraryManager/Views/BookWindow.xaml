<Window x:Class="LibraryManager.Views.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить/Редактировать книгу" Height="400" Width="600" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">
    <Grid Background="#F5F5F5" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Название -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtTitle" Padding="5" Margin="0,0,0,10"/>

        <!-- Автор -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Автор:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="ComboAuthor" Margin="0,0,0,10" Padding="5"/>

        <!-- Жанр -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Жанр:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="ComboGenre" Margin="0,0,0,10" Padding="5"/>

        <!-- Год издания -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Год издания:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,10"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtPublishYear" Padding="5" Margin="0,0,0,10"/>

        <!-- ISBN -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="ISBN:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,10"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtISBN" Padding="5" Margin="0,0,0,10"/>

        <!-- Количество -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Кол-во книг:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,10"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TxtQuantity" Padding="5" Margin="0,0,0,10"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Width="100" Click="Save_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button Content="Отмена" Width="100" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>